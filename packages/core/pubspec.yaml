name: flyingdarts_core
description: Contains the core of the application

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flyingdarts_shared:
    path: ./../shared
  flyingdarts_features:
    path: ./../features
    
  rxdart:
  web_socket_channel:
  flutter_bloc:
  amplify_flutter:

dev_dependencies:
  